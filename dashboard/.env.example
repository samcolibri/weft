# WeaveMind Dashboard Configuration

# Database URL for project/execution persistence (optional)
# If not set, the dashboard still works but data is lost on restart.
# When running via dev.sh, this is auto-set to the local PostgreSQL.
# DATABASE_URL=postgres://postgres:postgres@localhost:5433/weft_local
